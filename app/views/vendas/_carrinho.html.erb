
     Valor da Venda <%= @valorTotal %>
    <table class="table">
        <tr>
          <th>Nome</th>
          <th>Preço Venda</th>
          <th></th>
        </tr>
		<% @produtos.each do |p|%>
            <% produto = Produto.find(p) %>
		<tr>
			<th> <%= produto.modelo %></th>
    		<th> <%= produto.precoVenda %></th>
    		<th> </th>
    		<th> </th>
    		<th> </th>
		</tr>
		<% end %>

    </table>

    <%= link_to "Fechar Venda", :produtos => @produtos, :fechar => "Concluir" do %>
      <div class="btn btn-primary">
      Fechar Venda
      </div>
    <% end %> 

