
    
    <table class="table">
        <tr>
          <th>Nome</th>
          <th>Preço Venda</th>
          <th></th>
        </tr>
		<% @produtos.each do |p|%>
            <% produto = Produto.find(p) %>
		<tr>
			<th> <%= produto.modelo %></th>
    		<th> <%= produto.precoVenda %></th>
    		<th> </th>
    		<th> </th>
    		<th> </th>
		</tr>
		<% end %>

    </table>

    <%= link_to new_venda_path,:produtos => @produtos, :fechar => "Concluir", class: "btn btn-primary" %> 