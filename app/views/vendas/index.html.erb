<h1>Gerar Relatorio Vendas</h1>

<table>
  <thead>
    <tr>
      <th>Data</th>
      <th>Valortotal</th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @vendas.each do |venda| %>
      <tr>
        <td><%= venda.data %></td>
        <td><%= venda.valorTotal %></td>
        <td><%= link_to 'Show', venda %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>
<%= link_to "Gerar Relatorio", vendas_path(:format => "pdf") %>
<%= link_to 'New Venda', new_venda_path %>

